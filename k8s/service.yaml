apiVersion: v1
kind: Service
metadata:
  name: springboot-openshift-service
spec:
  selector:
    app: springboot-openshift
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort